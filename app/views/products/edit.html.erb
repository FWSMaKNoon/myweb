<% content_for :title, "Editing product" %>

<h3 class="mb-4 text-center">Chỉnh sửa</h3>

<%= render "form", product: @product %>

<br>

<div class="actions text-center">
  <%= link_to "Chi tiết", @product, class:"btn btn-secondary" %> 
  <%= link_to "Quay lại", products_path, class:"btn btn-dark" %>
</div>
